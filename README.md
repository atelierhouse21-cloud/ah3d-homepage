# AH3D 홈페이지 — 전체 최신 파일 (2026-09-23)

이번 zip에는 **저장소 전체(소스코드 + 이미지)**가 들어있습니다. GitHub에는 자동으로
올라가지 않았습니다 (이 작업 환경은 GitHub에 쓰기 권한이 없습니다) — 아래 방법대로
직접 저장소에 덮어써서 반영해주세요.

## 이번 zip에 포함된 수정 사항

1. **"이런 부품을 제작합니다" (PARTS) 섹션 삭제**
   - `components/home/PartsSection.tsx` 파일 자체를 삭제했습니다.
   - `app/page.tsx`에서 `<PartsSection />` 호출도 제거했습니다.
   - `app/globals.css`에서 관련 CSS(`.parts-grid`, `.parts-fig`, `.ann` 등)도 모두 삭제했습니다.
   - **주의**: 기존 저장소에 있던 `components/home/PartsSection.tsx` 파일은 이 zip에
     없으므로, 저장소에 남아있다면 직접 삭제해주세요 (그대로 두어도 어디서도
     import하지 않아 화면에는 영향 없지만, 정리 차원에서 지우는 것을 권장합니다).

2. **"02 3D MODELING" 서비스 행 — 데스크탑 정렬/크기 버그 수정**
   - 원인: `.svc-row.rev`가 이미지·텍스트 순서만 바꿔야 하는데 그리드 컬럼 폭
     자체(`1fr 1.25fr`)까지 다시 정의해서, 이미지가 좁은 컬럼에 들어가고 텍스트가
     넓은 컬럼을 차지하던 버그였습니다. (01, 03행과 다르게 02행만 이미지가
     작아 보이고 정렬이 어긋나 보였던 이유입니다.)
   - 수정: 컬럼 폭 재정의 줄을 삭제하고, `order`로 위치만 바꾸도록 했습니다.
     `app/globals.css`의 `.svc-row.rev` 관련 규칙을 확인하시면 됩니다.
   - 모바일은 원래부터 정상이었고 이번 수정과 무관합니다 (모바일은 flex 세로
     쌓기 구조라 데스크탑 그리드 버그의 영향을 받지 않았습니다).

## 반영 방법 (중요 — 전체 교체)

이 zip은 **현재 저장소의 전체 최신 상태**입니다. 부분적으로만 파일을 옮기면 CSS와
컴포넌트 구조가 어긋나서 사이트가 깨질 수 있으니, 아래 폴더/파일 전체를 이 zip의
내용으로 통째로 덮어써주세요.

- `app/` 전체
- `components/` 전체
- `lib/` 전체
- `public/hero/`, `public/portfolio/`, `public/quality/`
- `package.json`, `next.config.js`, `tsconfig.json`

`node_modules/`, `.next/`, `.git/`은 이 zip에 포함하지 않았습니다 (빌드 산출물이라
저장소에 올릴 필요 없는 파일들입니다).

## 아직 해결하지 않은 사항 (참고용)

- `app/about/page.tsx`, `app/services/page.tsx`와 `lib/content.ts`는 예전
  디자인 시스템(옛 "아틀리에 하우스" 브랜딩, 카드 UI)을 그대로 쓰는 페이지들입니다.
  메인 네비게이션에서는 연결되어 있지 않지만 주소로 직접 접속하면 보입니다.
  삭제할지, 새 디자인으로 다시 만들지 아직 결정하지 않은 상태라 이번에는
  건드리지 않았습니다.
- CTA 버튼("모델링 의뢰하기", "부품·시제품 상담하기" 등)은 아직 mailto: 링크로만
  연결되어 있습니다. 다음 작업(3번 항목)으로 실제 문의 페이지 형식을 먼저
  제안해드릴 예정입니다.

## 시작하기 (로컬 확인용)

1. Node.js 20.9 이상 설치
2. 이 폴더에서 `npm install`
3. `npm run dev` 실행 후 `http://localhost:3000` 접속
