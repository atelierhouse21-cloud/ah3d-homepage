# 업로드 안내

## 순서

1. `ah3d-homepage.zip` 의 압축을 풉니다.
2. GitHub의 `ah3d-homepage` 저장소를 엽니다.
3. `Add file` → `Upload files` 를 누릅니다.
4. 압축 푼 `ah3d-homepage` 폴더 **안으로 들어가서**, 내용물 전체를 선택해 끌어다 놓습니다.
   (폴더 자체를 끌지 마세요.)
5. `Commit changes` 를 누릅니다.

기존 파일은 자동으로 새 내용으로 덮어써집니다. 미리 지우실 필요 없습니다.

---

## 올린 뒤 확인할 것

Vercel이 자동으로 배포합니다. 1~2분 뒤 사이트를 열어 아래를 확인하세요.

- 포트폴리오 섹션에서 사진이 좌우로 넘어가는지
- PC에서 사진 위에 마우스를 올리면 좌우 화살표가 보이는지
- 휴대폰에서 손으로 밀면 사진이 한 장씩 멈추는지

---

## 사진 올리는 법

`public/portfolio/` 폴더에 아래 형식으로 올리시면 자동 반영됩니다.

```
01_정밀 기계 부품.jpg
02_경량화 설계.jpg
03_기능성 시제품.jpg
```

- 맨 앞 숫자는 순서만 정하고 화면에는 안 나옵니다.
- 그 뒤 글자가 사진 제목이 됩니다.
- 사진을 한 장이라도 올리면 현재 임시로 표시되는 타 사이트 사진은 전부 사라집니다.

권장 조건: JPG · 가로 1200px 내외 · 장당 300KB 이하 · 가로가 긴 비율

---

## 안 쓰는 파일에 대해

아래 파일들은 예전 디자인에 쓰였던 것들로, 지금은 메뉴에서 접근할 수 없습니다.
동작에는 아무 영향이 없어 그대로 두었습니다. 정리를 원하시면 말씀해 주세요.

```
app/about/page.tsx
app/services/page.tsx
app/portfolio/page.tsx
components/site/
lib/content.ts
lib/portfolio.ts
```
